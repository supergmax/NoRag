export function Footer() {
  return (
    <footer
      className="py-16 px-6 text-center text-sm"
      style={{
        borderTop: "1px solid rgba(255,255,255,0.05)",
        color: "var(--color-muted)",
      }}
    >
      NoRag — Markdown, not vectors. © 2026.
    </footer>
  );
}
